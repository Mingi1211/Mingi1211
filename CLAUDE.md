# CLAUDE.md — 세션 부트스트랩

> 이 파일은 매 턴 자동 로드된다. **짧게 유지할 것.** 상세 내용은 아래가 가리키는 파일에 둔다.

## 이 세션에서 가장 먼저 할 일

1. **`memory/STATE.md`를 읽는다.** 진행 중인 작업, 열린 질문, 다음 액션이 거기 있다.
2. 작업이 아래 중 하나에 해당하면 해당 스킬을 부른다.
   - 학교/팀 프로젝트 **주제 선정·기획·검증** → `project-ideation`
   - **발표 덱·프로토타입 목업·스크린샷** 제작 → `deck-factory`
   - 워드(docx) 과제·보고서를 **정해진 쪽수에 맞추기** → `docx-page-fit`
   - 이 기억 시스템 자체를 **운영·갱신** → `mingi-loop`
3. 사용자 고정 정보가 필요하면 `memory/PROFILE.md`.
   "이거 전에 왜 이렇게 정했지?"는 `memory/LEDGER.md`.

## 이 세션을 끝낼 때 할 일 (건너뛰지 말 것)

의미 있는 작업을 했다면 **커밋 전에**:

- `memory/STATE.md` 갱신 — 현재 상태·다음 액션·열린 질문
- 되돌릴 수 없는 판단을 했다면 `memory/LEDGER.md`에 **한 줄 추가** (append-only, 기존 줄 수정 금지)
- 다음에도 쓸 노하우·환경 제약을 알아냈다면 해당 `SKILL.md`에 반영

> 이 갱신이 이 시스템의 전부다. 갱신하지 않으면 다음 세션은 다시 1일차 신입이 된다.

## 절대 규칙

- 기억 파일에 **추측을 사실처럼 적지 않는다.** 미확인 정보는 `[미확인]` 표시를 붙인다.
- `memory/LEDGER.md`는 **덧붙이기만** 한다. 사실이 바뀌면 새 줄에 정정 기록을 남긴다.
- 개인정보·비밀번호·토큰은 기억 파일에 쓰지 않는다.

## 다른 프로젝트에서 이 기억을 쓰려면

→ `memory/BOOTSTRAP.md` 참고. **자동으로 따라오지 않는다. 연결 작업이 필요하다.**
